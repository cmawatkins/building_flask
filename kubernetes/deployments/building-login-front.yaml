apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: building-login-front
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: building-login-front
    spec:
      containers:
      - name: building-login-front
        image: pittcontainerreg.azurecr.io/building-login:redis-v1
        volumeMounts:
        - name: building-logs
          mountPath: /building_logs
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: 250m
          limits:
            cpu: 500m
        env:
        - name: REDIS
          value: "building-login-back"
      volumes:
      - name: building-logs
        emptyDir: {}
